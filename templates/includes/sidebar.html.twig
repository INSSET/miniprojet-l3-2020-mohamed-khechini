<div class="p-4">
        <h4 class="font-italic">Articles Récents</h4>
        <ol class="list-unstyled mb-0">
            {% for post in latests %}
                <li><a href="#">{{post.title}}</a></li>
            {% endfor %}
        </ol>
</div>