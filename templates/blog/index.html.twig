{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
    <div class="container">

        {% include "/includes/header.html.twig" %}

        <main class="container">
            <div class="p-4 p-md-5 mb-4 row text-white rounded bg-dark">
                <div class="col-md-6 px-0">
                    <h1 class="display-6 font-italic">Parlez de ce qui vous passionne, à votre manière</h1>
                    <p class="lead my-3">Multiple lines of text that form the lede, informing new readers quickly and efficiently about what’s most interesting in this Blog contents.</p>
                    <p class="lead mb-0"><a href="#" class="text-white fw-bold">Publications...</a></p>
                </div>
                <div class="col-md-6 px-0 panel">
                    <img src="/images/panel2.png" alt="Panel img" style="">
                </div>
            </div>

            <div class="row mb-2">
                <div class="col-md-8" style="padding-left: 0px">
                    {% for post in posts %}
                    <div class="row g-0 border rounded overflow-hidden flex-md-row mb-4 shadow-sm h-md-250 position-relative">
                        <div class="col p-4 d-flex flex-column position-static">
                            <h3 class="mb-0">{{ post.title }}</h3>
                            <div class="mb-1 text-muted">{{ post.time | date("\\L\\e Y-m-d \\à H:i")}}, par <a href="#" style="color: #55595c;">{{post.user.fullname}}</a></div>
                            <p class="card-text mb-auto">{{ post.body }}</p>
                            <a href="{{path('blog_show', {'slug': post.slug})}}" class="stretched-link">Lire la suite</a>
                        </div>
                        <div class="col-auto d-none d-lg-block">
                            <img src="{{ asset('uploads/' ~ post.picture) }}" width="280px" height="250px" alt="">               
                        </div>
                    </div>
                    {% endfor %}
                    <div class="">{{ knp_pagination_render(posts) }}</div>
                </div>
                <div class="col-md-4 info">
                    <div class="p-4 mb-3 bg-light rounded">
                        <h4 class="font-italic">Infos</h4>
                        <p class="mb-0">Etiam porta <em>sem malesuada magna</em> mollis zfqfezefzef ediojeoijfez Lorem ipsum dolor sit, amet consectetur adipisicing elit. Dicta aspernatur obcaecati fugiat. Minima, molestias aperiam! iojde. Curus sit amet fermentum. Aenean lacinia bibendum nulla sed consectetur.</p>
                    </div>

                    <div>
                        {{ include ("/includes/sidebar.html.twig", {'latests': latests})}}
                    </div>
                </div>
            </div>
        <main>
    </div>

    {% include "/includes/footer.html.twig" %}
{% endblock %}